{
  "compilerOptions": {
    "target": "ES6",
    "module": "ES6",
    "outDir": "./dist",
    "strict": true
  },
  "include": ["script.ts"]
}
// This just makes it easier to run the script with `tsc` command.
// And ensures that the TypeScript compiler uses the specified options